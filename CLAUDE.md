# iOS-PluginBase — Claude Instructions

## Session startup

When starting a session in this repository, load all files matching `.claude/skills/*/SKILL.md`.

## Skills

- `/minor-version-pr [feature name]` — Implement a minor version bridge method from SDK contract + native API, bump version, update CHANGELOG, and generate the feature branch locally.
